"""Application entrypoint package for launching the desktop authoring workflow.
These modules bridge process startup into the PySide6 GUI without owning project, runtime, or engine semantics.
Compilation, session planning, and execution contracts remain in the backend layers they call."""
